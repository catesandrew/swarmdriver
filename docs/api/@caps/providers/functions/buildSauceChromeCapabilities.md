# Function: buildSauceChromeCapabilities()

```ts
function buildSauceChromeCapabilities(opts?): object;
```

Defined in: providers/src/saucelabs/capabilities.ts:783

Build Sauce Chrome Capabilities

## Parameters

### opts?

[`SauceCapabilityValues`](../interfaces/SauceCapabilityValues.md) = `{}`

parsed settings to build from

## Returns

`object`

a slice to spread into a `capabilities` entry

### sauceOptions

```ts
sauceOptions: object = {};
```

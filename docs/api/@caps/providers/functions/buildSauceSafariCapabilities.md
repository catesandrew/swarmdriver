# Function: buildSauceSafariCapabilities()

```ts
function buildSauceSafariCapabilities(opts?): object;
```

Defined in: providers/src/saucelabs/capabilities.ts:759

Build Sauce Safari Capabilities

## Parameters

### opts?

[`SauceCapabilityValues`](../interfaces/SauceCapabilityValues.md) = `{}`

parsed settings to build from

## Returns

`object`

a slice to spread into a `capabilities` entry

### sauceOptions

```ts
sauceOptions: object = {};
```

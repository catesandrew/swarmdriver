# Function: listProviders()

```ts
function listProviders(): string[];
```

Defined in: packages/core/src/providers/registry.ts:128

Names of every currently-registered provider.

## Returns

`string`[]

registry keys, in registration order

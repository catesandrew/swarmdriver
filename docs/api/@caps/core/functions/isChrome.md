# Function: isChrome()

```ts
function isChrome(session?): Capability;
```

Defined in: packages/core/src/utils.ts:45

check if session is run by Chromedriver

## Parameters

### session?

[`SessionLike`](../interfaces/SessionLike.md) = `{}`

caps of session response

## Returns

[`Capability`](../interfaces/Capability.md)

the matching capability when run by Chromedriver

# Function: isFirefox()

```ts
function isFirefox(session?): Capability;
```

Defined in: packages/core/src/utils.ts:60

check if session is run by Geckodriver

## Parameters

### session?

[`SessionLike`](../interfaces/SessionLike.md) = `{}`

caps of session response

## Returns

[`Capability`](../interfaces/Capability.md)

the matching capability when run by Geckodriver

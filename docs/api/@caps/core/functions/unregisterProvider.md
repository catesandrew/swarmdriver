# Function: unregisterProvider()

```ts
function unregisterProvider(name): boolean;
```

Defined in: packages/core/src/providers/registry.ts:94

Remove a provider from the registry.

## Parameters

### name

`string`

registry key

## Returns

`boolean`

`true` when something was actually removed

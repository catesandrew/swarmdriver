# Function: waitForNativeAlertIsShown()

```ts
function waitForNativeAlertIsShown(reverse?): Promise<any>;
```

Defined in: packages/core/src/helpers/native/alert.ts:25

Wait for the alert to exist

## Parameters

### reverse?

`boolean` = `false`

if true it instead waits for the selector
to not match any elements.

## Returns

`Promise`\<`any`\>

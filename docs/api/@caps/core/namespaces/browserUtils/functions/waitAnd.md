# Function: waitAnd()

```ts
function waitAnd(options): Promise<any>;
```

Defined in: packages/core/src/helpers/browser/utils.ts:139

Waits for an element to be clickable.

## Parameters

### options

`any`

Options

## Returns

`Promise`\<`any`\>

Returns this

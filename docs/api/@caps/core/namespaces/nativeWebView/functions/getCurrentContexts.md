# Function: getCurrentContexts()

```ts
function getCurrentContexts(): Promise<any>;
```

Defined in: packages/core/src/helpers/native/web-view.ts:11

Returns an object with the list of all available contexts

## Returns

`Promise`\<`any`\>

An object containing the list of all available contexts

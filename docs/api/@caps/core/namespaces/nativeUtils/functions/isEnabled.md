# Function: isEnabled()

```ts
function isEnabled(element?): Promise<boolean>;
```

Defined in: packages/core/src/helpers/native/utils.ts:942

Give back if the element is enabled

## Parameters

### element?

[`ElementOrSelector`](../interfaces/ElementOrSelector.md) = `{}`

## Returns

`Promise`\<`boolean`\>

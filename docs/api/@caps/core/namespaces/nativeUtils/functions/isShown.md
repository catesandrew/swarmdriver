# Function: isShown()

```ts
function isShown(element?): Promise<boolean>;
```

Defined in: packages/core/src/helpers/native/utils.ts:892

Give back if the element is displayed

## Parameters

### element?

[`ElementOrSelector`](../interfaces/ElementOrSelector.md) = `{}`

## Returns

`Promise`\<`boolean`\>

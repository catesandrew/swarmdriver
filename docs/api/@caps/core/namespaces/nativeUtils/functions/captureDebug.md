# Function: captureDebug()

```ts
function captureDebug(desc?): Promise<void>;
```

Defined in: packages/core/src/helpers/native/utils.ts:1391

Capture some debug info in a failed test. The server logs, source and screenshot

## Parameters

### desc?

the test description

#### desc?

`string` = `''`

## Returns

`Promise`\<`void`\>

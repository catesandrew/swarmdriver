# Function: swipeOnDirection()

```ts
function swipeOnDirection(param?): Promise<any>;
```

Defined in: packages/core/src/helpers/native/gestures.ts:719

Swipe up/right/down/left based on a percentage.

## Parameters

### param?

[`SwipeOnDirectionOptions`](../interfaces/SwipeOnDirectionOptions.md) = `{}`

an optional params object

## Returns

`Promise`\<`any`\>

Returns a Promise

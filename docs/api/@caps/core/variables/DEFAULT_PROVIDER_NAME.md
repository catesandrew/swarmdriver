# Variable: DEFAULT\_PROVIDER\_NAME

```ts
const DEFAULT_PROVIDER_NAME: "saucelabs" = 'saucelabs';
```

Defined in: packages/core/src/providers/provider.ts:75

Provider used when `WDIO_PROVIDER` is not set.

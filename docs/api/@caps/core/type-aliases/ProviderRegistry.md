# Type Alias: ProviderRegistry

```ts
type ProviderRegistry = Record<string, Provider>;
```

Defined in: packages/core/src/providers/registry.ts:30

The registry store itself: provider implementations keyed by registry name.

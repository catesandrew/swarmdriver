# Function: setupSauceDesktopChrome()

```ts
function setupSauceDesktopChrome(__namedParameters?): object;
```

Defined in: src/services/remote-desktop-browser.js:302

Generate a config to test web applications in chrome, on x86 boxes, locally.

## Parameters

### \_\_namedParameters?

## Returns

`object`

Returns a config

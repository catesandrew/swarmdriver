# Function: setupLocaliOSBrowser()

```ts
function setupLocaliOSBrowser(__namedParameters?): object;
```

Defined in: src/services/local-device-browser.js:174

Generate a config to test web applications, on ios devices, locally.

## Parameters

### \_\_namedParameters?

## Returns

`object`

Returns a config

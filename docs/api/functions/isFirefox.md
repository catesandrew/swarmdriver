# Function: isFirefox()

```ts
function isFirefox(capabilities?): boolean;
```

Defined in: src/utils.js:34

check if session is run by Chromedriver

## Parameters

### capabilities?

`any` = `{}`

caps of session response

## Returns

`boolean`

Returns true if run by Chromedriver

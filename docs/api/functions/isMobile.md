# Function: isMobile()

```ts
function isMobile(capabilities?): boolean;
```

Defined in: src/utils.js:105

check if current platform is mobile device

## Parameters

### capabilities?

`any` = `{}`

caps

## Returns

`boolean`

Returns true if platform is mobile device

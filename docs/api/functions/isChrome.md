# Function: isChrome()

```ts
function isChrome(capabilities?): boolean;
```

Defined in: src/utils.js:20

check if session is run by Chromedriver

## Parameters

### capabilities?

`any` = `{}`

caps of session response

## Returns

`boolean`

Returns true if run by Chromedriver

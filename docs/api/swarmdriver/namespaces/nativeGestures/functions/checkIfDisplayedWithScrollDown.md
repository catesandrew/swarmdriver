# Function: checkIfDisplayedWithScrollDown()

```ts
function checkIfDisplayedWithScrollDown(element?): Promise<void>;
```

Defined in: src/helpers/native/gestures.js:307

Check if an element is visible and if not scroll down a portion of the screen to
check if it visible after a x amount of scrolls

## Parameters

### element?

`any` = `{}`

## Returns

`Promise`\<`void`\>

# Function: swipeItemLeft()

```ts
function swipeItemLeft(element): Promise<any>;
```

Defined in: src/helpers/native/gestures.js:967

Pinch or zoom an element (pinch doesn't work on Android with this method yet)

## Parameters

### element

`Element`

## Returns

`Promise`\<`any`\>

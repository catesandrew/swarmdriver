# Function: swipeToNextScreen()

```ts
function swipeToNextScreen(param?): Promise<any>;
```

Defined in: src/helpers/native/gestures.js:522

Just performs a swipe from an area near the right edge of the screen over to the left

## Parameters

### param?

an optional params object

#### duration?

`number` = `750`

duration of the swipe

#### landscape?

`boolean` = `false`

in horizontal landscape vs vertical portrait mode.

#### size?

`any`

value of device window size

## Returns

`Promise`\<`any`\>

Returns a Promise

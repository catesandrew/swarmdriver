# Function: hideSoftKeyboard()

```ts
function hideSoftKeyboard(element): void;
```

Defined in: src/helpers/native/utils.js:670

Hide the soft keyboard, but only if it is present

## Parameters

### element

`Element`

## Returns

`void`

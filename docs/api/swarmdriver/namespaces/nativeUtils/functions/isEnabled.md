# Function: isEnabled()

```ts
function isEnabled(element?): boolean;
```

Defined in: src/helpers/native/utils.js:869

Give back if the element is enabled

## Parameters

### element?

`Element` = `{}`

## Returns

`boolean`

# Function: isShown()

```ts
function isShown(element?): boolean;
```

Defined in: src/helpers/native/utils.js:819

Give back if the element is displayed

## Parameters

### element?

`Element` = `{}`

## Returns

`boolean`

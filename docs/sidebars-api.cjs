/**
 * Sidebar for the TypeDoc-generated API reference under `docs/api/`.
 *
 * This used to be a checked-in, fully-enumerated list of every generated page
 * (`api/functions/buildWdioConfig`, `api/classes/BrowserPage`, ...). That only
 * held while the repo was a single package emitting a flat `docs/api/` tree.
 * Now that TypeDoc runs with `entryPointStrategy: "packages"` it emits one
 * subtree per workspace package (`api/@caps/core/...`, `api/@caps/cli/...`),
 * so every hand-written path in the old file became a dangling reference the
 * moment `pnpm build-docs` ran.
 *
 * Autogenerating from the directory instead means the sidebar tracks whatever
 * TypeDoc actually produced -- adding a package or an export no longer
 * silently desynchronises this file from `docs/api/`.
 *
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
module.exports = {
  api: [
    {
      type: 'autogenerated',
      dirName: 'api',
    },
  ],
}
